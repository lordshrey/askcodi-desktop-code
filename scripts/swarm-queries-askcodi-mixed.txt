# Mixed-breadth query set for askcodi-desktop-code, designed to exercise routing.
# 4 NARROW queries (should route to Explore) + 4 BROAD queries (should route to PartitionWorker).
# Format: Q<n>|<label>|<query text>

# Narrow / single-area queries (expected: Explore)
Q1|narrow-find-binary|What does findSystemClaude in src/main/lib/claude/find-binary.ts do? Walk through its return logic.
Q2|narrow-db-schema|What tables does the Drizzle schema define? Just list each table and its columns from src/main/lib/db/schema/.
Q3|narrow-token-mgmt|How does the desktop app store and refresh OAuth tokens? Show me the auth-store.ts logic specifically.
Q4|narrow-trpc-router|What procedures does the projects tRPC router expose in src/main/lib/trpc/routers/projects.ts?

# Broad / cross-cutting queries (expected: PartitionWorker fanout)
Q5|broad-claude-integration|How does the Claude integration work end to end? Trace from user message in the renderer through to streaming output back to the UI, covering preload, main, and renderer.
Q6|broad-gaps|What features look stubbed out, mocked, or incomplete in this app? Look for TODOs, mock data, unconnected components, and unimplemented routes across the whole codebase.
Q7|broad-swarm-system|How does the swarm algorithm system work? What algorithms are available, how do they share memory, how does the orchestrator coordinate subagents, and how is the suite-runner wired together?
Q8|broad-data-flow|Trace the data flow when a user creates a new chat: input handler, IPC bridge, main-process router, database write, response back to renderer. Cover every layer.
